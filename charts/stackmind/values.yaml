replicaCount: 1

image:
  repository: ghcr.io/sigiuscom/stackmind
  tag: main
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 20m
    memory: 32Mi
  limits:
    cpu: 200m
    memory: 128Mi
